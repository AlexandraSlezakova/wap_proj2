e.forEach((function(e) {
                    hj.log.debug("Found new form.", "autotag", e),
                    hj.hq(e).on("submit.user_behavior_formsubmit", r)
                }
                
                tu to este obsahovalo hodnotu, ktora tam realne je
                
                                    // DO NOT APPEND MODULE SCRIPT
                    //f.appendChild(h),
                    
                    v session storage je last recording activity
                    
                    e -> co sa dialo
